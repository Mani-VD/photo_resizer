import { PhotodropzoneDirective } from './photodropzone.directive';

describe('PhotodropzoneDirective', () => {
  it('should create an instance', () => {
    const directive = new PhotodropzoneDirective();
    expect(directive).toBeTruthy();
  });
});
